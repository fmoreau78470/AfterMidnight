Commande,Description
git init                                # Initialiser un dépôt Git localement.
git add .                               # Ajouter tous les fichiers modifiés.
git status                              # Voir l'état des fichiers.
git commit -m "message"                 # Commiter les modifications avec un message.
git remote add origin URL               # Ajouter un dépôt distant.
git push -u origin main                 # Pousser le code sur GitHub pour la première fois.
git push                                # Pousser les modifications ultérieures.
git pull                                # Récupérer les modifications depuis GitHub.

git branch nom-de-la-branche            # créer une branche
git checkout nom-de-la-branche          # passer sur cette branche
git status                              # vérifier su quelle branche on est
git push -u origin nom-de-la-branche#   # publier ta nouvelle branche

git log --oneline                       # liste les commits
git restore .                           # remplace tous les fichiers modifiés par leur version du dernier commit
git checkout a1b2c3d -- .               # restaure les fichiers de ce commit 


git branch nom_branche                  # Créer une nouvelle branche
git checkout -b nom_branche             # Créer et basculer dessus
git branch                              # Lister les branches locales
git branch -a                           # Montre les branches sur ta machine
git checkout nom_branche                # Basculer vers une autre branche
git status ou git branch                # Voir sur quelle branche on est. Le * indique la branche active

git branch -d nom_branche               # Supprimer une branche locale (fusionnée)
git branch -D nom_branche               # Supprimer une branche locale (forcé)

source venv/bin/activate
python3.12 -m src.main
